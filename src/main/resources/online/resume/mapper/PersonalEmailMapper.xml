<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="online.resume.mapper.PersonalEmailMapper">
    <insert id="savePersonalEmail" parameterType="online.resume.model.PersonalEmail">
    insert into personalemail(lookername,lookeremail,lookermessage,sendtime) value(#{lookerName},#{lookerEmail},#{lookerMessage},#{sendTime})
        </insert>
</mapper>